<div class="topbar">
  <a href="/">
    <img 
        class="logo" 
        alt="Logo of the site, consists of the letters L and K" 
        src="../../../images/logo.svg" 
        width="30px" 
        heigth="30px"
    >
  </a>
  <span>
    <a href="https://github.com/lenakujath/personal-website">Source</a>
  </span>
</div>
<header id="hero">
    <nav class="nav">
      <ul class="nav-links">
        {% for item in nav.pages %}
        <li>
          <a href="{{ item.url }}" {{ helpers.getLinkActiveState(item.url, page.url) | safe }}>
            <span>{{ item.label }}</span>
          </a>
        </li>
        {% endfor %}
      </ul>
    </nav>
    <div class="title">
      <figure style="{{ style }}">
        <a style="{{ hlink }}" href="{{ href }}">
          <picture>
            <img fetchpriority="high" alt="{{ alt }}" src="{{ image }}">
            <span class="{{ class }}"></span>
          </picture>
        </a>
      </figure>
      <h1>{{ title }}<span class="index-symbol"> {{symbol}}</span></h1>
    </div>
</header>